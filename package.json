{
  "name": "ffmpeg-merge-api",
  "version": "1.0.0",
  "description": "Simple API to merge image and audio into video",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "multer": "^2.0.0"
  }
}
